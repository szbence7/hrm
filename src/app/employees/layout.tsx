export const metadata = {
  title: 'Employees - HRM System',
  description: 'Manage your employees',
};

export default function EmployeesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 